[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[service]
name = "agentgatepay-tx-signer"

[[env]]
name = "PRIVATE_KEY"
description = "Your wallet private key (starts with 0x)"
type = "SECRET"
required = true
